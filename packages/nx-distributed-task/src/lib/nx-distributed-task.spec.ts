import { nxDistributedTask } from './nx-distributed-task';

describe('nxDistributedTask', () => {
  it('should work', () => {
    expect(nxDistributedTask()).toEqual('nx-distributed-task');
  });
});
